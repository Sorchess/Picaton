# URL внутреннего backend API
# В docker-compose используется имя сервиса: http://main-app:8000
# Для локальной разработки без Docker: http://localhost:8000
BACKEND_URL=http://main-app:8000

# API-ключи для доступа к MCP-серверу (через запятую)
# Если пусто — сервер открыт (только для dev/internal network!)
# Генерация ключа: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Пример: MCP_API_KEYS=key-2gis-AbCdEf123,key-claude-XyZ456
MCP_API_KEYS=

# Хост и порт MCP-сервера
MCP_HOST=0.0.0.0
MCP_PORT=8001
